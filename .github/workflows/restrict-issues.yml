name: Restrict Issue Creation

on:
  issues:
    types: [opened]

jobs:
  enforce-role-only:
    runs-on: ubuntu-latest
    steps:
      - name: Check User Permissions
        id: check_auth
        uses: actions/github-script@v7
        with:
          script: |
            const username = context.payload.issue.user.login;
            try {
              const { data: permission } = await github.rest.repos.getCollaboratorPermissionLevel({
                owner: context.repo.owner,
                repo: context.repo.repo,
                username: username
              });
              
              // Valid permissions: 'admin', 'maintain', 'write', 'read', 'none'
              // Check if the user's permission level is one of the allowed roles
              const allowedRoles = ['admin', 'maintain', 'write'];
              const isAuthorized = allowedRoles.includes(permission.permission);
              
              core.setOutput('is_authorized', isAuthorized);
            } catch (error) {
              // If the user isn't even a collaborator, the API throws an error
              core.setOutput('is_authorized', false);
            }

      - name: Close and Redirect Issue
        if: steps.check_auth.outputs.is_authorized == 'false'
        uses: actions/github-script@v7
        with:
          script: |
            const issueNumber = context.issue.number;
            
            // 1. Post a redirection comment
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issueNumber,
              body: `👋 Hi @${context.payload.issue.user.login}! Issue creation is restricted to maintainers and administrators. Please move this over to our **Discussions** tab so the community can help you!`
            });

            // 2. Close the issue
            await github.rest.issues.update({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issueNumber,
              state: 'closed',
              state_reason: 'not_planned'
            });

            // 3. Lock the issue to prevent further spam
            await github.rest.issues.lock({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issueNumber,
              lock_reason: 'resolved'
            });
